{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "Retrieve savings income",
  "description": "This endpoint allows a developer to retrieve savings income for securities or foreign interest. A National Insurance number and tax year must be provided.",
  "type": "object",
  "properties": {
    "securities": {"$ref": "../definitions.json#/definitions/securities"},
    "foreignInterest": {"$ref": "../definitions.json#/definitions/foreignInterest"},
    "links": {
      "type": "array",
      "items": {
        "type": "object",
        "description": "A list of endpoint links that indicate possible actions related to the current resource",
        "properties": {
          "href": {
            "description": "The relative url of the endpoint",
            "type": "string",
            "example": "/individuals/income-received/savings/{nino}/{taxYear}"
          },
          "rel": {
            "description": "A label for the endpoint, which describes how it is related to the current resource. The rel will be self where the action is retrieval of the same resource",
            "type": "string",
            "enum": [
              "self",
              "amend-savings-income",
              "delete-savings-income"
            ]
          },
          "method": {
            "description": "The HTTP method type for the endpoint",
            "type": "string",
            "enum": [
              "GET",
              "PUT",
              "DELETE"
            ]
          }
        },
        "required": [
          "href",
          "rel",
          "method"
        ]
      }
    }
  }
}
